aliyunId

roleName

```
$scope.srcrdsOtherOptions = {
	      url: '/rds/list.json',
	      placeholder: i18nHelper.i18n('dts.common.message.choice.rds'),
        dataHandler: function (term, page) {
			  	return {
				region : $scope.formData.srcRegion || '',
				instanceId : term ? term : '',
				pageSize : 10,
				currentPage : page,
				aliyunId : vm.aliyunId,
				roleName : vm.roleName
			  	};
  		  },
	      formatResult:fmtComboxRow
	    }
```

